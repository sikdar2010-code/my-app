{
  "name": "backend",
  "version": "1.0.0",
  "description": "MERN backend with auth, OTP, security",
  "main": "server.js",
  "type": "module",

  "scripts": {
    "dev": "npx kill-port 5000 && nodemon server.js",
    "start": "npx kill-port 5000 && node server.js",
    "kill": "npx kill-port 5000",
    "restart": "npm run kill && npm run dev"
  },

  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^17.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^8.3.1",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.3",
    "mongodb": "^7.1.0",
    "mongoose": "^8.0.0",
    "morgan": "^1.10.1"
  },

  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}